import { Header as HeaderElement } from 'antd/es/layout/layout';

function Header() {
  return (
    <HeaderElement style={{ color: '#fff', textAlign: 'center' }}>
      Reviewer Finder
    </HeaderElement>
  );
}

export default Header;
